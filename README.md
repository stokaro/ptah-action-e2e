# Ptah Action E2E

Sibling repository used to prove that `stokaro/ptah-action@v1` writes sticky
pull request migration-plan comments and destructive-change Check Runs in a
real pull request workflow.
